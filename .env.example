# Command to run. Must output newline-delimited JSON events.
# Example (serial, recommended): use a stable by-id path if possible
# To list: `taptap list-serial-ports`
TAPTAP_CMD="taptap observe --serial /dev/serial/by-id/usb-FTDI_FT232R_USB_UART_XXXX-if00-port0"

# InfluxDB 1.x (DB=bms)
INFLUX_URL=http://127.0.0.1:8086
INFLUX_DB=bms
# Leer lassen = Default Retention Policy der DB (bei dir: `autogen` = unbegrenzt)
INFLUX_RP=
INFLUX_MEASUREMENT=tigo_power_report
INFLUX_DRY_RUN=0

# Logging
LOG_LEVEL=INFO

# Optional: Healthcheck MQTT status messages (OK/CRIT)
TIGO_HEALTH_MQTT_ENABLED=0
TIGO_HEALTH_MQTT_HOST=127.0.0.1
TIGO_HEALTH_MQTT_PORT=1883
TIGO_HEALTH_MQTT_TOPIC=tigo/health
TIGO_HEALTH_MQTT_USER=
TIGO_HEALTH_MQTT_PASS=
TIGO_HEALTH_MQTT_RETAIN=1
